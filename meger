   web-stat-filter:
        enabled: true
        url-pattern: /*
        exclusions: "*.js,*.gif,*.jpg,*.png,*.css,*.ico,/druid/*"

      # 慢 SQL 配置
      filters: stat,wall
      stat:
        slow-sql-millis: 2000   # 超过 2 秒算慢 SQL
        log-slow-sql: true
        merge-sql: true